import React from "react";

const AddBtnSquare = ({onPress}) => {

    return (
        <button type='button' onClick={onPress} className='addBtnSquare'>+ Add Title</button>
    );
}

export default AddBtnSquare;
